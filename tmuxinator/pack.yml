name: pack

windows:
<% if @args[0] %>
  - main:
      root: ~/.local/share/nvim/site/pack/github/opt/<%= @args[0] %>
      layout: main-horizontal
      panes:
        -
        -

  - vroom:
      root: ~/.local/share/nvim/site/pack/github/opt/<%= @args[0] %>/vroom
      panes:
        -

<% end %>

  - maktaba:
      root: ~/.local/share/nvim/site/pack/github/opt/vim-maktaba
      layout: main-horizontal
      panes:
        -
        -

  - vroom:
      root: ~/.local/share/nvim/site/pack/github/opt/vroom
      layout: main-horizontal
      panes:
        -
        -

